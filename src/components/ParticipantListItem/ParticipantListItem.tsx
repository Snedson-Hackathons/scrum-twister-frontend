import { observer } from 'mobx-react-lite';
import './ParticipantListItem.scss';

export const ParticipantListItem = observer(() => {
  return <div className='participant-list-item'></div>;
});
